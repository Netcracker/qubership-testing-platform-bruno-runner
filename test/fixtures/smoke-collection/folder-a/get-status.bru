meta {
  name: Get Status
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/status/200
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
